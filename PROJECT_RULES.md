# PROJECT_RULES.md — Mission Control

## Scope
- Mission Control is a separate repo/product from Dispatch.

## Active directives
1. Brand identity should reflect Jarvis (primary) and Pip (sub-bot).
2. Read-only by default unless explicitly approved otherwise.
3. Keep startup optional/off by default; run only when requested remotely.
4. Prefer OpenClaw runtime-derived data for this dashboard.
5. When working in this project, always propose optimization opportunities, better execution routes, and next-step recommendations.
## Change control
- Non-destructive edits allowed under global R1 policy.
- Ask before destructive structure changes.
